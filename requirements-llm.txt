# LLMOps Dependencies for Milestone 2
# ==================================

# LLM Provider - Google Gemini
google-generativeai>=0.3.0,<1.0.0

# LangChain for RAG Pipeline (Bonus)
langchain>=0.1.0,<0.3.0
langchain-google-genai>=0.0.6,<1.0.0
langchain-community>=0.0.10,<0.3.0

# Vector Store - FAISS for local indexing
faiss-cpu>=1.7.4,<2.0.0

# PyTorch (CPU-only, required for sentence-transformers)
# NOTE: On Windows, if you encounter DLL errors, install PyTorch CPU separately:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Or skip sentence-transformers - the code has fallback embeddings
# torch>=2.0.0,<3.0.0  # Commented out - install separately if needed

# LlamaIndex for advanced retrieval (Bonus)
llama-index>=0.9.0,<0.12.0
llama-index-llms-gemini>=0.1.0,<1.0.0
llama-index-embeddings-huggingface>=0.1.0,<1.0.0

# Sentence Transformers for embeddings (optional - code has fallback)
# Install separately if PyTorch is available:
#   pip install sentence-transformers
# sentence-transformers>=2.2.0,<3.0.0

# Evaluation Metrics
rouge-score>=0.1.2,<1.0.0
nltk>=3.8.0,<4.0.0
sacrebleu>=2.3.0,<3.0.0

# Guardrails (optional - code has keyword fallback)
# presidio-analyzer>=2.2.0,<3.0.0
# presidio-anonymizer>=2.2.0,<3.0.0
# detoxify>=0.5.0,<1.0.0  # Optional - has keyword fallback

# Text processing
tiktoken>=0.5.0,<1.0.0
unstructured>=0.10.0,<1.0.0

# Rate limiting
ratelimit>=2.2.1,<3.0.0

# Caching
cachetools>=5.3.0,<6.0.0
diskcache>=5.6.0,<6.0.0

# Additional monitoring
opentelemetry-api>=1.20.0,<2.0.0
opentelemetry-sdk>=1.20.0,<2.0.0
